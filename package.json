{
  "devDependencies": {
    "@biomejs/biome": "2.3.8"
  },
  "name": "ormx",
  "private": true,
  "scripts": {
    "build": "bun run --filter '*' build",
    "check": "biome check --write",
    "test": "bun run --filter '@ormx/filters' build && bun run --filter '*' test",
    "typecheck": "bun run --filter '@ormx/filters' build && bun run --filter '*' typecheck"
  },
  "workspaces": [
    "packages/*"
  ]
}
